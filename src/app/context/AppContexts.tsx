/*
Use this for using context as the state manager. Otherwise, this file may be deleted.
*/

// import { UserClientType } from "@/test-data/types";
// import { createContext } from "react";

// type UserContextType = {
//   user: UserClientType | null;
//   setUser: (user: UserClientType) => void;
// };

// export const UserContext = createContext<UserContextType>({
//   user: null,
//   setUser: () => {},
// });
// export const ThemeContext = createContext<"light" | "dark">("dark");
