type ToolbarConfigType = {
  categoryItems: string[];
  filterItems: string[];
  height: number;
};

const config: ToolbarConfigType = {
  height: 16,
  categoryItems: ["One", "Two", "Three", "Four", "Five"],
  filterItems: ["one", "two", "three", "four", "five"],
};

export default config;